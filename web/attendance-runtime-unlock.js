/* Marker used by the parent Employee runtime to restore the native V40 navigation without touching the attendance form. */
(function(window){'use strict';window.__MAGASIN_NATIVE_SHOWVIEW=window.showView;})(window);
