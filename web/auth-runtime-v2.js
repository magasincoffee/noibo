/* MAGASIN AUTH RUNTIME V2 */
(function(){'use strict';
const sb=window.supabase.createClient('https://menvbzlsncmpuvnaifxa.supabase.co','sb_publishable_HsvCS6HDZnCDInd9PUoh0g_V34wJVqx',{auth:{autoRefreshToken:true,persistSession:true,detectSessionInUrl:true}});
const $=id=>document.getElementById(id),status=$('status');
const msg=(t,e=false)=>{if(status){status.textContent=t||'';status.classList.toggle('error',e)}};
const view=n=>{document.querySelectorAll('.auth-view').forEach(v=>v.classList.toggle('active',v.id==='view-'+n));if(n!=='login')msg('')};
const route=role=>location.replace(['STAFF','EMPLOYEE'].includes(String(role||'STAFF').toUpperCase())?'./secure-app-runtime.html?target=employee':'./secure-app-runtime.html?target=manager');
document.querySelectorAll('[data-auth-view]').forEach(a=>a.onclick=e=>{e.preventDefault();view(a.dataset.authView)});
document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>{const i=document.querySelector(b.dataset.toggle);if(i)i.type=i.type==='password'?'text':'password'});
$('togglePassword')?.addEventListener('click',()=>{$('password').type=$('password').type==='password'?'text':'password'});
$('forgot')?.addEventListener('click',e=>{e.preventDefault();view('forgot');const u=$('username').value.trim();$('forgotEmail').value=u.includes('@')?u:'';$('forgotEmail').focus()});
async function loadProfile(id){const {data,error}=await sb.from('profiles').select('role,status').eq('id',id).single();if(error)throw error;return data}
$('loginForm')?.addEventListener('submit',async e=>{e.preventDefault();const b=$('submit'),u=$('username').value.trim(),p=$('password').value;if(!u||!p){msg('Vui lòng nhập tên đăng nhập và mật khẩu.',true);return}b.disabled=true;b.textContent='Đang đăng nhập…';try{let email=u;if(!u.includes('@')){const {data,error}=await sb.rpc('resolve_login_email',{p_username:u});if(error)throw error;if(!data)throw new Error('Tên đăng nhập không tồn tại hoặc chưa được kích hoạt.');email=data}const {data,error}=await sb.auth.signInWithPassword({email,password:p});if(error)throw new Error('Tên đăng nhập hoặc mật khẩu không đúng.');const pr=await loadProfile(data.user.id);if(String(pr.status).toUpperCase()!=='ACTIVE'){await sb.auth.signOut();throw new Error('Tài khoản chưa được kích hoạt.')}route(pr.role)}catch(x){msg(x.message||'Đăng nhập thất bại.',true)}finally{b.disabled=false;b.textContent='Đăng nhập'}});
$('registerForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,b=f.querySelector('button[type=submit]');const d={full:$('regFullName').value.trim(),phone:$('regPhone').value.trim(),email:$('regEmail').value.trim(),user:$('regUsername').value.trim(),pass:$('regPassword').value,confirm:$('regConfirm').value};if(!d.full||!d.email||!d.user||!d.pass)return msg('Vui lòng nhập đầy đủ thông tin bắt buộc.',true);if(d.user.length<3||!/^[A-Za-z0-9._-]+$/.test(d.user))return msg('Tên đăng nhập từ 3 ký tự, chỉ gồm chữ, số, dấu . _ -.',true);if(d.pass.length<8)return msg('Mật khẩu phải có ít nhất 8 ký tự.',true);if(d.pass!==d.confirm)return msg('Mật khẩu nhập lại không khớp.',true);b.disabled=true;b.textContent='Đang đăng ký…';try{const {data,error}=await sb.auth.signUp({email:d.email,password:d.pass,options:{emailRedirectTo:location.origin+location.pathname+'?auth=verify',data:{username:d.user,full_name:d.full,phone:d.phone}}});if(error)throw error;f.reset();view('login');msg(data.session?'Đăng ký thành công. Tài khoản đang chờ quản lý kích hoạt.':'Đăng ký thành công. Hãy kiểm tra email xác nhận.')}catch(x){msg(x.message||'Không thể đăng ký.',true)}finally{b.disabled=false;b.textContent='Đăng ký'}});
$('forgotForm')?.addEventListener('submit',async e=>{e.preventDefault();const b=e.currentTarget.querySelector('button[type=submit]'),email=$('forgotEmail').value.trim();if(!email)return msg('Vui lòng nhập email.',true);b.disabled=true;b.textContent='Đang gửi…';try{const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:location.origin+location.pathname+'?auth=reset'});if(error)throw error;msg('Đã gửi hướng dẫn đặt lại mật khẩu. Hãy kiểm tra cả thư rác.')}catch(x){msg(x.message||'Không thể gửi email.',true)}finally{b.disabled=false;b.textContent='Gửi hướng dẫn'}});
$('resetForm')?.addEventListener('submit',async e=>{e.preventDefault();const b=e.currentTarget.querySelector('button[type=submit]'),p=$('resetPassword').value,c=$('resetConfirm').value;if(p.length<8)return msg('Mật khẩu mới phải có ít nhất 8 ký tự.',true);if(p!==c)return msg('Mật khẩu nhập lại không khớp.',true);b.disabled=true;b.textContent='Đang cập nhật…';try{const {data}=await sb.auth.getSession();if(!data.session)throw new Error('Liên kết khôi phục không còn hiệu lực.');const {error}=await sb.auth.updateUser({password:p});if(error)throw error;await sb.auth.signOut();history.replaceState({},document.title,location.pathname);view('login');msg('Đã cập nhật mật khẩu. Bạn có thể đăng nhập bằng mật khẩu mới.')}catch(x){msg(x.message||'Không thể cập nhật mật khẩu.',true)}finally{b.disabled=false;b.textContent='Cập nhật mật khẩu'}});
sb.auth.onAuthStateChange(e=>{if(e==='PASSWORD_RECOVERY')view('reset')});const q=new URLSearchParams(location.search);if(q.get('auth')==='reset')view('reset');else if(q.get('auth')==='verify')msg('Email đã được xác nhận. Tài khoản sẽ chờ quản lý kích hoạt.');else sb.auth.getSession().then(async({data})=>{if(!data.session)return;try{const p=await loadProfile(data.session.user.id);if(String(p.status).toUpperCase()==='ACTIVE')route(p.role)}catch(e){}});
})();
